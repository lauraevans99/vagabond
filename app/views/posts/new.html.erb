<div class="col-md-6 col-md-offset-3 text-center" id="posts_holder">
	<form method="post" action="/users/<%= @user.id %>/posts">
		<legend>Add Post</legend>
		<%= form_for [@user, @post] do |f| %>
		<% flash.each do |name, msg| -%>
	      <%= content_tag :div, msg, class: name %>
	    <% end -%>
		<div class="form-group">
			<label class="col-sm-12 col-md-12 col-lg-12 col-xl-12 control-label" for="">Title</label>
			<%= f.text_field :title, placeholder: "Title", class: "form-control input-md" %>
		</div>
		<div class="form-group">
			<label class="col-sm-12 col-md-12 col-lg-12 col-xl-12 control-label" for="">Body</label>
			<%= f.text_area :body, placeholder: "Body", class: "form-control input-md" %>
		</div>
		<div class="form-group">
			<label class="col-sm-12 col-md-12 col-lg-12 col-xl-12 control-label" for="">City</label>
			<%= f.collection_select(:city_id, City.all, :id, :name, {:class => "form-control input-lg"}) %>
		</div>
			<%= f.hidden_field :user_id, :value => @user.id %>
			<button type="submit" class="btn btn-primary">Add Post</button>
			<!-- <%= f.submit "Add Post" %> -->
		<% end %>
	</form>
</div>